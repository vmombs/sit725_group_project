<!DOCTYPE html>
<html lang="en">
<head>
  <title>Dashboard | Sneezl</title>
  <meta charset="UTF-8">
  <link rel="icon" type="image/x-icon" href="/assets/favicon.ico">
  <link rel="shortcut icon" type="image/x-icon" href="/assets/favicon.ico">
  <!--Link directly to the jQuery CDN-->
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>

  <!--Import Google Icon Font-->
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

  <!--Import materialize.css-->
  <link type="text/css" rel="stylesheet" href="css/materialize.min.css" media="screen,projection" />

  <!-- Import custom material colours -->
  <link type="text/css" rel="stylesheet" href="css/material-styles.css" media="screen,projection" />

  <!--Let browser know website is optimized for mobile-->
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <style>
    .group {
      margin-top: 2.5rem;
    }
  </style>
</head>

<body>
  <%- include('../partials/navbar') %>

  <main class="container">
    <h3>Hello <%= user.username %></h3>
    <div class="group">
      <h5>Your predicted symptoms today <a href="">→</a></h5>
      <div class="row">
        <% data.symptoms.forEach(symptom => { %>
          <%- include ('../partials/dashboard-card', { data: symptom }) %>
        <% }) %>
      </div>
    </div>
    <div class="group">
      <h5>On days like this, you took <a href="">→</a></h5>
      <div class="row">
        <% data.antihistamines.forEach(antihistamine => { %>
          <%- include ('../partials/dashboard-card', { data: antihistamine }) %>
        <% }) %>
      </div>
    </div>
  </main>
</body>

</html>